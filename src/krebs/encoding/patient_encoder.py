"""Patient encoder: bundle of bind(gene_id, level) over selected genes.

Placeholder for Week 2. Plan: per-gene role hypervectors generated once,
per-patient hypervector = bundle(bind(role_g, level(expr_g)) for g in genes).
"""
